<div class="confirm mb-5 pb-5">
  <div class="row">
    <div class="col-md-10 mx-auto mb-5">
      <h5 class="my-4 col-md-2 py-1 text-center offset-md-1">注文履歴一覧</h5>

      <table class="table table-bordered table-sm mb-5">
        <tr class="table-active">
          <td>注文日</td>
          <td class="col-5">配送先</td>
          <td class="col-3">注文商品</td>
          <td>支払額</td>
          <td>ステータス</td>
          <td>注文詳細</td>
        </tr>
        <% @orders.each do |order| %>
          <tr>
            <td><%= order.created_at.strftime("%Y/%m/%d") %></td>
            <td>
              <%= order.postal_code %><br>
              <%= order.address %><br>
              <%= order.name %>
            </td>
            <td>
              <% order.order_details.each do |order_detail| %>
                <%= order_detail.item.name %>
              <% end %>
            </td>
            <td><%= order.total_price %></td>
            <td><%= order.status_i18n %></td>
            <td><%= link_to "表示する", order_path(order.id), class: "btn btn-primary btn-sm px-3" %></td>
          </tr>
        <% end %>
      </table>
    </div>
  </div>
</div>
